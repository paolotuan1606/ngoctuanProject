const stateDefault = {};

export const AdminReducer = (state = stateDefault, action) => {
    switch (action.type) {
        // case "ADD_USER": {

        // }

        default:
            return { ...state };
    }
}